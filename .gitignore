/node_modules
/.next/
/out/
/build
.DS_Store
*.log
*.tsbuildinfo
/.next-stable/

# env
.env
.env.local
.env*.local

# per-user account tastes (local best-effort; Vercel uses cookie)
.data/
.data/account-tastes/

# KPI 18 admin export dumps — interne, never commit
*gouts-internes*.csv
cc-gouts-internes*.csv

# Vercel CLI (projectId/orgId — do not publish)
.vercel/

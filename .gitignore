/node_modules
/.next/
/out/
/build
.DS_Store
*.log
*.tsbuildinfo
/.next-stable/

# env
.env
.env.local
.env*.local

# per-user account tastes (local best-effort; Vercel uses cookie)
.data/
.data/account-tastes/

# KPI 18 / P1 admin export dumps — interne, never commit
*gouts-internes*.csv
cc-gouts-internes*.csv
cc-tastes-*.csv
cc-tokens-*.csv
cc-rsvps-*.csv
cc-visits-*.csv

# hashed real-user bench snapshots — interne only, never commit
bench-results/real30/
bench-internal/real30/

# Vercel CLI (projectId/orgId — do not publish)
.vercel/
